# docker build -t riscv-gnu-toolchain:latest-llvm-musl -f riscv/Dockerfile.llvm.musl .

FROM riscv-gnu-toolchain:latest-src

ENV DEBIAN_FRONTEND=noninteractive
ENV RISCV=/opt/riscv

# riscv-gnu-toolchain
WORKDIR /home/riscv-gnu-toolchain
RUN ./configure --prefix=$RISCV --with-arch=rv64gc --with-abi=lp64d --enable-llvm
RUN make -j `nproc` musl
RUN make install

# update PATH
ENV PATH="/opt/riscv/bin:${PATH}"
